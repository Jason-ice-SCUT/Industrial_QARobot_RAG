# ========== 镜像源配置（关键调整：PyTorch 源优先） ==========
# 优先从 PyTorch 官方 CUDA 11.8 源下载（解决 +cu118 版本超时）
--index-url https://download.pytorch.org/whl/cu118
# 其他基础库从清华源兜底（加速非 PyTorch 依赖）
--extra-index-url https://pypi.tuna.tsinghua.edu.cn/simple

# ========== PyTorch 2.1.2 + CUDA 11.8（修复cudnn版本） ==========
torch==2.1.2+cu118
torchvision==0.16.2+cu118
torchaudio==2.1.2+cu118

# 核心依赖（模型加载与微调）
transformers==4.38.2
peft==0.10.0
torch==2.1.2
accelerate==0.27.2
tokenizers==0.15.2
safetensors==0.4.2

# 数据集处理
datasets==2.18.0
pyarrow==15.0.0
pandas==2.2.2
numpy==1.26.4

# 工具依赖（日志、进度条等）
tqdm==4.66.2
colorama==0.4.6
filelock==3.13.1


# Hugging Face 镜像支持
huggingface-hub==0.23.0

# 可选依赖（如需推理时使用）
sentencepiece==0.2.0
protobuf==3.20.3